# Project authorization

The owner explicitly authorized agents on 2026-09-07 to commit, push and publish completed changes in this project without asking again each time.

- Use the existing GitHub remote `hebecked/Home-Workout` and the existing Cloudflare Pages project `home-workout`.
- Test the requested changes before publishing; report any unverified or failed checks.
- Preserve unrelated changes and do not publish credentials, private feedback exports or generated test artifacts.
- This authorization does not cover destructive Git operations, changing hosting providers/domains, purchasing services or weakening security.
